server {
    listen 443 ssl;
    server_name api.gametrackr.localhost;

    ssl_certificate     /etc/ssl/private/cert.crt;
    ssl_certificate_key /etc/ssl/private/cert.key;

    location / {
        proxy_pass http://gametrackr-backend:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# server {
#     listen 80;
#     server_name vgb.ntkia.es;
#     return 301 https://vgb.ntkia.es$request_uri;
# }

# server {
#     server_name vgb.ntkia.es;
#     listen       443 ssl http2;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:20m;
#     ssl_session_timeout 180m;
#     ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
#     ssl_ciphers EECDH+CHACHA20:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;

#     ssl_certificate /etc/ssl/private/cert.crt;
#     ssl_certificate_key /etc/ssl/private/cert.key;
#     location / {
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#         proxy_pass http://gestion-back:80;
#         proxy_set_header Host $host;
#         proxy_redirect http:// https://;
#     }
# }
